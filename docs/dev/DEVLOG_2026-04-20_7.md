# Dev Log — 2026-04-20 (7)

主线目标：将 escape-room level3 纳入离线回归矩阵，验证在失败注入下仍可推进到通关（provider=mock）。

## Changes

- Mock provider policy: added deterministic solve path for escape-room level3.
  - Uses tool+args matching and `_phase` markers to disambiguate repeated `walk.to` directions.
  - File: `snakes/runtime/mock_stream.py`

- Tests: added level3 integration test for provider=mock.
  - File: `tests/test_runner_mock.py`

- Regression: executed a level3 seed matrix and recorded report.
  - Report: `docs/dev/RUN_REPORT_mock_matrix_level3_2026-04-20.md`

## Tests

- `pytest`: all passing (148 tests)

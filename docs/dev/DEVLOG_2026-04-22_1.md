# Dev Log — 2026-04-22 (1)

Context: Level 3 runs are now succeeding under seed+failure injection, but the agent still wastes turns because `status.` omitted puzzle metadata. Models often rely on status to decide whether to call `arm.use` vs explore; missing puzzle info leads to incorrect statements like "no puzzles listed" while exits are blocked by puzzles.

## Change

- `apps/hackathon/x2_mock.py`
  - `status.` now includes `puzzles: [{index, description, solved}]` for the current room.

## Expected impact

- Reduce unnecessary exploration/loops when an exit is blocked.
- Faster time-to-solve (time-first) and fewer `unknown` failures from wrong attempts.

## Tests

- `pytest`: pass

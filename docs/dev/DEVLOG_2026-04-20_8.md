# Dev Log — 2026-04-20 (8)

主线目标：让 `score` 输出能直接回答“慢在哪里/失败在哪里”，为后续主线优化提供抓手。

## Changes

- Toolchain: timeout ToolOutcome now includes `metrics.latency_ms` estimate.
  - File: `snakes/loop.py`

- `snakes score`: added minimal latency breakdown.
  - `tool_latency_ms_total`: sum of `metrics.latency_ms`
  - `tool_latency_by_group`: latency bucketed by tool group prefix (camera/arm/walk/...)
  - `failure_latency_ms`: latency sum bucketed by failure_type
  - File: `snakes/cli.py`

- Tests: extended score regression to assert new fields exist.
  - File: `tests/test_score_breakdown.py`

## Tests

- `pytest`: all passing (148 tests)

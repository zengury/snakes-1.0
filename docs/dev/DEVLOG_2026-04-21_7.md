# Dev Log — 2026-04-21 (7)

Context: Level 3 success run (`b5065f8b0950`) still spent extra turns guessing the lab password. The notice board presents `3-1-4`, but the puzzle solution is stored as `314`, so the natural first attempt `3-1-4` fails and creates unnecessary `unknown` failures.

## Change

- `apps/hackathon/escape_room.py`
  - `Puzzle.attempt()`: if the puzzle solution is digits-only, normalize the answer by stripping non-digits (e.g. `3-1-4` → `314`) before comparing.

## Expected impact

- Faster Level 3 runs (time-first) by eliminating common formatting mismatch.
- Reduced `unknown: Wrong answer` noise during password entry.

## Tests

- `pytest`: pass

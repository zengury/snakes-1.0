# Dev Log — 2026-04-20 (6)

主线目标：减少卡死与无意义循环；将 recover 从“提示”升级为更稳定的推进机制；建立轻量回归矩阵。

## Changes

- Runner: added minimal anti-loop safeguard. If the same tool+args repeats >=4 times, inject a recovery follow-up.
  - File: `snakes/runtime/runner.py`

- Runner: recovery follow-up text adjusted to include template recovery steps (system/perception/manipulation).
  - File: `snakes/runtime/runner.py`

- Tools: added optional `group` field to AgentTool (for future safeguards/coordination).
  - Files: `snakes/types.py`, `snakes/scenarios/escape_room_mock.py`

- Regression: added `scripts/run_matrix_mock.py` to run small seed matrices under probabilistic failures.
  - Report: `docs/dev/RUN_REPORT_mock_matrix_2026-04-20.md`

## Tests

- `pytest`: all passing (147 tests)

# Dev Log — 2026-04-21 (4)

Focus: real LLM escape-room reliability and time-to-solve by removing common tool/observation friction.

## Changes

- head.look: allow looking at inventory items (e.g., note) in X2 mock.
  - File: `apps/hackathon/x2_mock.py`

- Scenario observation: include arm holdings + head_target in `scenario.observe()` to reduce extra status calls.
  - File: `snakes/scenarios/escape_room_mock.py`

- Tools: add `head.scan` (room scan) and `arm.release` (explicit drop) to reduce ambiguous actions.
  - File: `snakes/scenarios/escape_room_mock.py`

## Tests

- `pytest`: pass

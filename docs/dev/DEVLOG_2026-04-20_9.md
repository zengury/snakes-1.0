# Dev Log — 2026-04-20 (9)

Focus: make the golden path robust with *real* LLM providers (anthropic/openai), not just provider=mock.

## Change

- Runtime: if assistant ends a turn without any tool_use while the scenario is not done, inject a `[Continue]` follow-up (autonomy-safe) to prompt tool usage.
  - Limits: max 3 injections per run; resets streak when a tool_use occurs.
  - Logs an event `continue_injected` to EventLog for audit.
  - File: `snakes/runtime/runner.py`

## Rationale

Real models sometimes respond with narrative/plan and stop tool calling prematurely. This guard keeps the run moving without human steering.

# Dev Log — 2026-04-20 (3)

主线目标：继续强化 escape-room 黄金路径的可回归性（不依赖外部 API key），并在失败注入存在时保持可推进。

## Changes

- Failure injection: added minimal forced-failure knobs to make deterministic regression tests possible.
  - `force_vision_fail`, `force_manip_fail`, `force_system_timeout`, `force_system_disconnect`
  - File: `snakes/scenarios/failure_injection.py`

- Mock provider policy: made it step-success-driven (retries until each required step succeeds), matching failure-first reality.
  - It matches tool args (e.g. interact table vs interact blue_cup) to avoid false progress.
  - File: `snakes/runtime/mock_stream.py`

- Tests: added integration test ensuring level2 can still succeed under a small set of forced failures.
  - File: `tests/test_runner_mock.py`

- Added a tiny helper script for local regression runs.
  - File: `scripts/run_regression_mock.sh`

## Tests

- `pytest`: all passing (143 tests)

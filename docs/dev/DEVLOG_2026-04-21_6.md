# Dev Log — 2026-04-21 (6)

Context: First Level 3 run (`c1d3cf2e83c2`) failed with long-horizon loops in Abandoned Lab. Root causes:

- Exit blocking was opaque: `walk.to` returned only "A puzzle blocks this exit" with no hint which puzzle.
- `arm.use` tool prompt implied inventory item usage; Level 3 includes code/password puzzles (e.g. "314") that are not inventory items.
- Observation did not include puzzle descriptions, forcing the model to guess indices/answers.

## Changes

- `apps/hackathon/escape_room.py`
  - `move()`: when an exit is blocked, return `blocking_puzzles` with indices + descriptions.
  - `solve_puzzle()`: on "No such puzzle" return `available_puzzles`; on "Wrong answer" return `puzzle` metadata (index/description/hints_available).

- `snakes/scenarios/escape_room_mock.py`
  - `observe()`: include `puzzles` list (index/description/solved) in observation.
  - Tool schema: clarify `arm.use` can take `answer` (free-form code string), not only an inventory `item`.

- `apps/hackathon/x2_mock.py`
  - `arm.use`: accept `answer` as an alias for `item`.

## Tests

- `pytest`: pass

# Dev Log — 2026-04-20 (2)

主线目标：让 `AgentLoop → Tools → Observe/Verify → Recover → EventLog` 更稳、更少卡死；不扩展无关细节。

## Changes

- Toolchain semantics (minimal): added `timeout_s` and `max_retries` to `AgentTool`.
  - Execution loop now supports retry-on-system-failure and optional timeout via `asyncio.wait_for`.
  - Retry attempts are recorded into tool result metrics (`metrics.attempts`) and `retry_history`.
  - Files: `snakes/types.py`, `snakes/loop.py`

- Escape-room mock tools: set default timeouts and `max_retries=1`.
  - File: `snakes/scenarios/escape_room_mock.py`

- Observability: `snakes watch` shows attempts; `snakes score` adds `timeouts` and `retry_attempts_total`.
  - File: `snakes/cli.py`

- Test: added regression for retry behavior.
  - File: `tests/test_toolchain_retry.py`

## Tests

- `pytest`: all passing (142 tests)

## Notes

- This is intentionally minimal: only system-type retry is automatic; perception/manipulation recovery remains agent-driven via `[Recovery]` follow-up.

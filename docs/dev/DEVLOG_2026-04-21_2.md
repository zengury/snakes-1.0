# Dev Log — 2026-04-21 (2)

Focus: real LLM robustness on escape-room level2 (reduce failure modes caused by inventory/container semantics).

## Observation

A real Anthropic run failed by grabbing a container (blue_cup) before interacting with it. The key (brass_key) remained hidden inside the cup and became unreachable, causing long-horizon confusion.

## Change

- EscapeRoom.interact(): when interacting with an item in inventory, if it contains items, reveal and *extract* contents into inventory and return `found`.
  - This makes container semantics consistent and prevents an irreversible dead-end.
  - File: `apps/hackathon/escape_room.py`

## Tests

- `pytest`: pass

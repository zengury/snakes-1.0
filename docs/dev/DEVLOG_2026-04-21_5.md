# Dev Log — 2026-04-21 (5)

Fix: `head.scan` returned a raw `EscapeRoom.look()` dict without `ok=True`, so the runtime classified it as `failure_type=unknown` even though it contained useful content. This produced wasted turns and spurious unknown failures.

## Change

- `apps/hackathon/x2_mock.py`
  - For `head.look` with empty target (room scan) and `head.scan`, return a proper tool result:
    - `{ok: true, result: {room, description, visible_objects, exits}}`

## Tests

- `pytest`: pass

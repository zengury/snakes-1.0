# Dev Log — 2026-04-20 (10)

Focus: improve robustness with real LLM providers and reserve a place for on-device/local OpenAI-compatible models.

## Changes

- Runtime: `[Continue]` injection now includes a fresh observation snapshot (and logs it) when the model stops without tool_use while scenario not done.
  - File: `snakes/runtime/runner.py`

- LLM client: OpenAI provider now supports `OPENAI_BASE_URL` (OpenAI-compatible local server / on-device small model endpoint).
  - Files: `snakes/llm_client.py`, `snakes/runtime/runner.py`

## Tests

- `pytest`: pass

# Dev Log — 2026-04-21 (1)

Focus: improve real LLM run quality (time-to-solve) and fix provider plumbing.

## Changes

- LLMClient: plumb `max_tokens` through Anthropic/OpenAI streaming calls (was hardcoded to 4096 for Anthropic).
  - File: `snakes/llm_client.py`

- CLI/Runner: add `--max-tokens` (default 1024) and pass into Agent/LLM stream.
  - Files: `snakes/cli.py`, `snakes/runtime/runner.py`, `snakes/runtime/llm_adapter.py`

- Escape-room: classify "Cannot see ..." failures as `failure_type=perception` (was unknown).
  - File: `snakes/scenarios/escape_room_mock.py`

## Rationale

The successful Anthropic run took ~158s wall-clock primarily due to model latency + verbose responses. Lower per-turn max tokens reduces latency and encourages concise tool-focused behavior.

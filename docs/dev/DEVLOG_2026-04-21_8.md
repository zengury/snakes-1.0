# Dev Log — 2026-04-21 (8)

Context: Level 3 seeded run with failure injection (`203d1b870727`) failed in Control Room with long-horizon loops.

Key issues observed:
- Model tried `arm.grab access_code` in Control Room; tool failed because `grab` only works for room objects, while `access_code` is already in inventory.
- Model used `arm.use item=access_code` but puzzle expects the literal answer string `helix`; the model often fails to re-read the access_code text in long-horizon runs.

## Changes (time-first, minimal semantics)

- `apps/hackathon/escape_room.py`
  - `pickup()`: include `description` in the success payload to surface new information immediately (e.g. access code content), reducing extra look calls.

- `apps/hackathon/x2_mock.py`
  - `arm.grab`: if pickup fails with "No object named ... here" but the object exists in inventory, treat it as "equipping" from inventory (returns ok=true + equipped=true) and set arm holding.

## Tests

- `pytest`: pass

# Dev Log — 2026-04-20

目标：保持主线优先（run → tool → verify/observe → recover → eventlog），避免沉迷字段细节。

## Changes

- EventLog: tool_result 现在写入完整 ToolOutcome 字段（outcome/failure_type/phenomenon/retryable/metrics + tool/args/result）。
  - Files: `snakes/memory_bridge.py`, `snakes/cli.py`

- Runtime: 在 tool 失败且 retryable 时，系统自动注入 `[Recovery] ...` follow-up（非人类 steering；每 turn 限制一次）。
  - Files: `snakes/runtime/runner.py`

- Scenario: Escape-room mock 在关键动作后调用 `check_escape()`，保证 `scenario.is_done()` 能正确反映通关状态。
  - File: `snakes/scenarios/escape_room_mock.py`

- Offline mainline: 增加 provider=mock 的离线 stream_fn，使得主线可在无 API key 下跑通（用于测试/回归）。
  - Files: `snakes/runtime/mock_stream.py`, `snakes/runtime/runner.py`, `snakes/cli.py`
  - Added test: `tests/test_runner_mock.py`

## Tests

- `pytest`: all passing (141 tests)

## Notes

- ToolOutcome 的目标是“支撑主线可恢复与可复盘”，保持最小结构；后续优化优先放在 recover/verify 与工具组合效率上。

# Dev Log — 2026-04-20 (5)

主线目标：保持 runtime kernel 语义与应用层 prompt 解耦；escape-room prompt 归场景可配置。

## Changes

- Scenario API: added optional application-layer `prompt_instructions()`.
  - File: `snakes/scenarios/base.py`

- Escape-room mock: implemented `prompt_instructions()` with a short scenario hint.
  - File: `snakes/scenarios/escape_room_mock.py`

- Runner: runtime kernel prompt is scenario-agnostic; scenario hint injected into first user prompt.
  - Kernel rules are minimal invariants.
  - File: `snakes/runtime/runner.py`

## Tests

- `pytest`: all passing (147 tests)

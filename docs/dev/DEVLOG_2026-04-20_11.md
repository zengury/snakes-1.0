# Dev Log — 2026-04-20 (11)

Focus: real LLM run readiness (Anthropic tool naming constraints).

## Problem

Anthropic rejects tool names containing dots (e.g. `camera.get`):

`tools.0.custom.name: String should match pattern '^[a-zA-Z0-9_-]{1,128}$'`

## Change

- Added deterministic tool-name encoding in `snakes/runtime/llm_adapter.py`.
  - Maps internal names (camera.get, arm.interact, status.) to provider-safe names.
  - Applies mapping both when sending tool schemas AND when converting message history.
  - Maps incoming tool calls back to internal names so execution works.

## Tests

- `pytest`: pass

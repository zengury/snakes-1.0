# Dev Log — 2026-04-21 (3)

Focus: improve real LLM robustness on escape-room level2 by removing avoidable semantic traps.

## Observed failure mode (seed=44)

The model grabbed `blue_cup` before interacting with it, then never revealed `brass_key` and spiraled into long-horizon confusion (wrong `arm.use` attempts, wrong puzzle_index, etc.).

## Changes

1) EscapeRoom.pickup(): if a picked object is a container, reveal and drop contents into the room (and return `found`).
   - Prevents irreversible dead-end where a hidden item is trapped inside a carried container.

2) EscapeRoom.solve_puzzle(): forgiving behavior — if there's exactly one puzzle, clamp invalid `puzzle_index` to 0.
   - Reduces "No such puzzle" loops caused by LLM guessing indices.

3) X2HackathonMock head.look: treat target="room" as a scan (empty target).
   - Reduces common LLM perception failures on room scanning.

Files:
- `apps/hackathon/escape_room.py`
- `apps/hackathon/x2_mock.py`

## Tests

- `pytest`: pass
